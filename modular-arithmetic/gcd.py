import math
print(math.gcd(66528, 52920))



'''
GCD = biggest number that divides both numbers.

b < a → always coprime (if a is prime)
b > a → maybe divisible → not guaranteed

Two numbers are coprime if their GCD = 1.

'''