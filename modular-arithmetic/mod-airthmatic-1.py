a = 11 % 6
b = 8146798528947 % 17
print(min(a, b))

'''
11÷6=1 remainder 5

The Congruence modulo relatuon can be transformed to a modulo operation:

a ≡ b mod m <=> a % m = b
Because as the description says: a ≡ b mod m means
 that when we divide the integer a by m, the remainder 
 is b, so b is the remainder of the euclidean division
   between a and m.
'''